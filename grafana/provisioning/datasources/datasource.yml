apiVersion: 1

datasources:
  # Default Loki data source for SIEM pipeline dashboard
  - name: Loki
    type: loki
    access: proxy
    url: http://loki:3100
    isDefault: true
    editable: false
    uid: loki-siem-datasource
